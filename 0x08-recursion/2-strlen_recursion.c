#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen_recursion - returns the length of a string
 * @s: string to be tested
 * Return: length of s
 */
int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
