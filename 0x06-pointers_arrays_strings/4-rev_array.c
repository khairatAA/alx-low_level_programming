#include <stdio.h>
#include "main.h"

/**
 * reverse_array - reverses the content of an array
 * @a: array to be tested
 * @n: integer
 */
void reverse_array(int *a, int n)
{
	int i;

	for (i = n; i >= n; i--)
	{
		_putchar(a[i]);
	}
}
